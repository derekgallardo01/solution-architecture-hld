repos:
  - repo: local
    hooks:
      - id: validate-templates
        name: Validate templates (placeholder + checkbox linter)
        entry: python validate.py
        language: system
        pass_filenames: false
        always_run: true
        stages: [pre-commit]
